import { CeasarCipherPipe } from './ceasar-cipher.pipe';

describe('CeasarCipherPipe', () => {
  it('create an instance', () => {
    const pipe = new CeasarCipherPipe();
    expect(pipe).toBeTruthy();
  });
});
